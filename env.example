# Base de Datos
# Para desarrollo local usando SQLite
DATABASE_URL="file:./dev.db"

# Para producción con PostgreSQL (reemplazar con tu URL real)
# DATABASE_URL="postgresql://username:password@localhost:5432/clickup_clone"

# NextAuth - IMPORTANTE: Cambiar en producción
# Generar una clave secreta segura con: openssl rand -base64 32
NEXTAUTH_SECRET="CAMBIAR-ESTA-CLAVE-SECRETA-EN-PRODUCCION"
NEXTAUTH_URL="http://localhost:3000"

# Google OAuth (Opcional)
# Obtener en: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID="tu-google-client-id-aqui"
GOOGLE_CLIENT_SECRET="tu-google-client-secret-aqui"

# Email (Opcional, para notificaciones futuras)
# Configurar con tu proveedor de email
EMAIL_SERVER="smtp://usuario:contraseña@smtp.ejemplo.com:587"
EMAIL_FROM="noreply@tudominio.com"

# IMPORTANTE:
# 1. Nunca subas el archivo .env.local al repositorio
# 2. Cambia todas las claves secretas en producción
# 3. Usa variables de entorno en el servidor de producción 